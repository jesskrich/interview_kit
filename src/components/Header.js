import React from 'react';
import Logo from '../imgs/jopwell.png'

const Header = () => {
  return (
    <header>
      <img src={Logo} className='logo' />
    </header>
  )
}

export default Header;
